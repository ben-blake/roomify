# Local development dependencies — no GPU required.
# Install with: pip install -r requirements-dev.txt
#
# The full Colab runtime has torch, diffusers, etc. pre-installed.
# This subset lets you run pure-Python tests and the CLI locally.

PyYAML>=6.0.1
pandas==2.2.2
Pillow>=10.2.0
typer>=0.12.0
pytest>=7.4.0
pytest-cov>=4.1.0
