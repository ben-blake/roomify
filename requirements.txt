# Colab GPU runtime (2026.01) already provides the full ML stack.
# See: https://github.com/googlecolab/backend-info/blob/main/pip-freeze.gpu.txt
#
# Key pre-installed versions (do NOT re-pin these tightly):
#   torch 2.10.0+cu128, torchvision 0.25.0+cu128, numpy 2.0.2
#   diffusers 0.37.1, transformers 5.0.0, accelerate 1.13.0
#   opencv-python-headless 4.13.0.92, Pillow 11.3.0
#   pandas 2.2.2, matplotlib 3.10.0, huggingface-hub 1.10.1
#   typer 0.24.1
#
# This file only installs packages NOT in the Colab runtime,
# or sets safe floors that won't downgrade what Colab already has.

# ── Packages absent from Colab — must install ───────────────────────────────
controlnet-aux>=0.0.7
open_clip_torch>=2.24.0
lpips>=0.1.4

# ── Web app (not in Colab by default) ───────────────────────────────────────
streamlit>=1.31.0

# ── Tunnel fallback (primary is cloudflared binary) ─────────────────────────
pyngrok>=7.0.0

# ── CLI — Colab has typer 0.24.1; floor only, no upper cap ──────────────────
typer>=0.12.0

# ── Config parsing ──────────────────────────────────────────────────────────
PyYAML>=6.0.1

# ── Testing ─────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0

# ── Notebooks ───────────────────────────────────────────────────────────────
ipywidgets>=8.0.0
